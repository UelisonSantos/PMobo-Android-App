<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uelis.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="PMOBO"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".ListeningDataInicio" />
        <activity android:name=".ListeningNomeProjeto" />
        <activity android:name=".ListeningEPT" />
        <activity android:name=".ListeningNomeProjTarefa" />
        <activity android:name=".ListeningNomeTarefa" />
        <activity android:name=".ListeningTipoProjeto" />
        <activity android:name=".CriandoProjeto" />
        <activity android:name=".CriandoTarefa" />
    </application>

</manifest>